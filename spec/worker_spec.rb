require 'spec_helper'

describe CF::Worker do
  # context "create a worker" do
  #   it "the plain ruby way" do
  #     worker = CF::Worker.new()
  #     worker.count.should eq(1)
  #   end
  # end
end